import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  form: {
    marginTop: "1rem",
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "center",
    gap: "1rem",
  },
}));
